using System;
namespace FlipZon.Utilities
{
    public enum MessageType
    {
        Postive=1,
        Negative=2,
    }
    public enum FingerPrintStatus
    {
        Success = 1,
        Failure = 2,
    }
}

